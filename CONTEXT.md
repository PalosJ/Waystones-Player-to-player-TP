# Waystones Player-to-player TP

本词汇表固定 Waystones Player-to-player TP 在全部加载器与 Minecraft 目标中的领域语言，避免把上游实现细节误当成产品语义。

## 维护与发布

**Canonical main（主基线）**：
永久代表 Minecraft 1.21.1 / Java 21 / NeoForge 原设计，也是其他目标共享行为的语义来源。
_避免使用_：最新分支、通用分支

**统一加载器分支**：
按加载器和重大 API 线聚合正式目标的维护线，即两条 `1.21.x` 或两条 `26.x` 分支。26.x 与 1.21.x 不共享已编译 common，也不混合 Java 21/25 或旧 requirement/Shogi 上下文。
_避免使用_：版本大杂烩、通用分支

**正式目标**：
拥有明确游戏与依赖边界、可安装产物及对应验证证据的一项受支持 Minecraft/加载器组合。
_避免使用_：试验构建、能编译的版本

**共享 1.21.2/1.21.3 目标**：
在同一加载器下由一份产物同时服务 Minecraft 1.21.2 和 1.21.3、并分别完成两版验收的正式目标。
_避免使用_：1.21.3 专用版、宽版本兼容版

## 玩家目录与目的地

**Listed 玩家（可列出玩家）**：
当前连接中由 Minecraft 的 listed 玩家流提供给客户端目录的在线玩家；这是展示边界，不等同于服务端授权。
_避免使用_：所有在线玩家、把 listed 流当作服务端权限

**服务端在线目标解析**：
服务端按请求 UUID独立解析当前在线玩家。客户端 listed 状态只决定目录展示，不构成服务端传送权限；本模组不承诺阻止自定义数据包猜测第三方隐藏玩家 UUID。
_避免使用_：把 `allowsListing()` 描述为玩家间传送授权

**玩家目录**：
Warp Stone 界面左侧实时呈现的其他 Listed 玩家集合。
_避免使用_：玩家快照、展开层

**玩家目的地**：
以目标玩家当前所在位置表示、仅供本次传送使用且不进入 Waystones 数据库的目的地。
_避免使用_：玩家 Waystone、永久传送点

**Waystones 原生玩家落点**：
Waystones 优先选择目标方块的相邻空位，没有相邻候选时回退到目标方块中心；本附属不增加安全门禁。
_避免使用_：安全落点、绝对安全位置、附属模组落点扫描

## 请求与结算

**Warp Stone 使用绑定**：
当前菜单所对应的确切 Warp Stone 与使用手；它是服务端接受玩家目的地请求的必要上下文。
_避免使用_：同类物品、客户端物品声明

**确认成功**：
Waystones 接受发送者的传送，且服务器确认发送者实际移动到目的地区域或事件重定向位置。
_避免使用_：已调用传送、API 返回成功

**玩家传送经验模式**：
玩家目的地对 Waystones 经验规则的选择，取值为 `NEVER`、`FOLLOW_WAYSTONES` 或 `ALWAYS`。
_避免使用_：免费模式开关、Waystones 全部费用

**固定耐久结算**：
每次确认成功后对已绑定 Warp Stone 施加一次 1 点原生耐久损耗；创造模式遵从原生豁免。
_避免使用_：Waystones 耐久配置、预扣耐久

**锁定 Shogi 上下文**：
26.x 玩家传送事务中固定玩家、手、Warp Stone、目标、费用规则输入与执行器边界的上下文；允许上游事件取消、增加实体或准备任务，但任何费用替换、执行器覆盖或费用敏感字段修改都会关闭失败。
_避免使用_：可编辑费用上下文、复用任意 Shogi 效果
