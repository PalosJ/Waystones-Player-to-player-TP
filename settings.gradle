pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            name = 'NeoForge'
            url = uri('https://maven.neoforged.net/releases')
        }
        maven {
            name = 'TencentCloudFallback'
            url = uri('https://mirrors.cloud.tencent.com/nexus/repository/maven-public/')
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'waystonesptpt'

include('core')
include('runtime:neoforge-smoke')

def neoForgeTargets = [
        'neoforge-26.1',
        'neoforge-26.1.1',
        'neoforge-26.1.2',
        'neoforge-26.2',
]
neoForgeTargets.each { targetId ->
    include("targets:${targetId}")
    project(":targets:${targetId}").buildFileName = '../../gradle/neoforge-target.gradle'
}
